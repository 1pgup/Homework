#include <stdio.h>

#include "making_format_string.h"

/* char *making_format_string(char *format_string, enum sizes size_of_array) {
    snprintf(format_string, FORMAT_STRING_MAX_SIZE-1, "%%%ds", size_of_array-1);
    return format_string;
} */

